#install de vue cli
$ npm install -g @vue/cli@next

#create the app
$ vue create github-app

#add pwa support
$ vue add pwa

#building th app
$ npm run build

#install browser-sync to serve pwa
$ npm install -g browser-sync

#enter into dist folder and run browser-sync
$ browser-sync